/**
 * Options page script.
 * Currently minimal — settings will be expanded in future phases.
 */

// Future: load and save settings from chrome.storage.sync
console.log('JSON Spark options page loaded');
