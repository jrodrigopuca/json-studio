/**
 * Raw view component types.
 */

export interface RawViewOptions {
  /** Whether to show line numbers. */
  showLineNumbers?: boolean;
  /** Whether content is editable. */
  editable?: boolean;
}
